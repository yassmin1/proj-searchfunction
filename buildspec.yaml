version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.11
  build:
    commands:
      - aws cloudformation package --template-file search-gateway.yaml --s3-bucket docsearch-artifact  --output-template-file gateway.yaml

artifacts:
  type: zip
  files:
    - search-gateway.yaml
    - gateway.yaml
